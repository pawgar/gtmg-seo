<%= form_for(@kpi_objective, remote: true) do |f| %>

<div id="error_explanation" class="bg-danger text-danger">
 </div> 
      
      <div class="form-group">
        <%= f.label :title, "<i class='fa fa-align-justify margin-r-5'> </i> Title".html_safe %>
        <%= f.text_area :title, class: 'form-control', rows: '4' %>
      </div>
      

  <div class="actions">
     <%= f.button  @kpi_objective.new_record? ? "Create" : "Update", :class => "btn btn-primary", data: {disable_with: "<i class='fa fa-spinner fa-spin'></i> Please wait..."} %>
     <%#= f.button "Proceed", :class => "btn btn-primary", data: {disable_with: "<i class='fa fa-refresh fa-spin'></i> Please wait..."} %>
  
  </div>
<% end %>
