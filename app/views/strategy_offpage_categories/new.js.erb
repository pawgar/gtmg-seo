// Add the dialog title
$('#dialog h3').html("<i class='fa fa-fw fa-plus'></i> Assign Category");
 
// Render the edit form
$('.modal-body').html('<%= j render("assign_category_form") %>');
 
// Show the dynamic dialog
$('#dialog').modal("show");
 
// Set focus to the first element
$('#dialog').on('shown.bs.modal', function () {
	  $('.first_input').focus()
  })
